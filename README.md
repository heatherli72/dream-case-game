# Dream Case Party Show V7

Standalone GitHub Pages build for `dream_case_party_show_v7.html`.

The site entrypoint is `index.html`. It includes the game UI and embedded MP3 audio as a single self-contained HTML file.
